const module = () => {
    const inputLogin = document.querySelector('login');
    const inputPassword = documet.querySelector('pass');

    inputPassword.addEventListener('input', (event) => {
        
    })

    inputLogin.addEventListener('input', (event) => {
        
    })
}

export default module;